add more text files to be folder "jobs"
each file should be a separate job posting


python version 3.8
install requirements using below command
pip install -r requirements.txt


run main code
streamlit run main.py

